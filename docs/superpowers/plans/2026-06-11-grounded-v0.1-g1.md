# grounded v0.1 (G-1 읽기 선행 규칙) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 안 읽은 파일을 Edit/Write 하려는 시도를 PreToolUse hook이 exit 2로 결정론적으로 차단하는 Claude Code 가드레일 v0.1.

**Architecture:** 세 개의 Python hook 스크립트(SessionStart 초기화 / PostToolUse 적립 / PreToolUse 검문)가 `.grounded/ledger.json` 하나를 공유한다. hook은 무상태 독립 프로세스이므로 ledger가 유일한 상태다. 개발·검증은 `.claude/settings.json` 직접 등록으로 하고(플러그인 hook 버그 회피, 명세 08-5), 저장소는 처음부터 플러그인 표준 레이아웃(`.claude-plugin/plugin.json` + `hooks/hooks.json`)으로 구성해 출시 시 그대로 패키징한다.

**Tech Stack:** Python 3 stdlib only (의존성 0), unittest(+subprocess로 hook을 실제 stdin JSON 인터페이스로 테스트), Claude Code hooks (PreToolUse/PostToolUse/SessionStart).

**핵심 설계 결정 (명세 대비):**
- ledger는 **cwd당 단일 파일** (세션별 분리 안 함). resume 시 session_id 변경으로 근거가 증발하면 오탐(false block)이 나는데, 명세 철학이 "오탐 < 누락"이므로 동시 세션 교차 오염(누락)을 감수한다. 한계로 문서화.
- SessionStart `source`가 `resume`/`compact`면 ledger 유지, `startup`/`clear`면 초기화. (대화가 기억하는 근거를 날리면 오탐.)
- Write는 **대상 파일이 없으면 통과** (새 파일 생성은 읽을 수 없으므로 근거 불요). 기존 파일 덮어쓰기만 검문.
- ledger가 **손상**(corrupt JSON)이면 fail-open(통과). ledger가 **없으면** 빈 ledger로 간주해 차단(post_record가 자동 생성하므로 없음 = 아무것도 안 읽음).
- 경로는 `os.path.realpath`로 정규화 (상대/절대/symlink 통일. macOS `/var`→`/private/var` 주의).
- hook 내부 오류는 절대 사용자 흐름을 깨지 않는다: post_record/session_start는 항상 exit 0, pre_gate는 판정 불가 시 exit 0.

---

## File Structure

```
grounded/
├── .claude-plugin/plugin.json   # 플러그인 매니페스트 (출시용)
├── .claude/settings.json        # 개발용 hook 직접 등록 (명세 08-5)
├── hooks/
│   ├── hooks.json               # 플러그인용 hook 등록 (${CLAUDE_PLUGIN_ROOT})
│   ├── ledger_io.py             # 공유: ledger 로드/원자적 저장/경로 정규화
│   ├── session_start.py         # SessionStart: ledger 초기화/유지
│   ├── post_record.py           # PostToolUse: 근거 적립 (관찰 전용)
│   └── pre_gate.py              # PreToolUse: G-1 검문 (exit 2 차단)
├── tests/
│   ├── hook_runner.py           # 헬퍼: hook을 subprocess+stdin JSON으로 실행
│   ├── test_session_start.py
│   ├── test_post_record.py
│   └── test_pre_gate.py
├── grounded-spec.html           # 설계 명세 (기존)
└── CLAUDE.md                    # 프로젝트 컨텍스트 (기존)
```

---

### Task 1: 저장소 스캐폴드 + 플러그인 매니페스트

**Files:**
- Create: `.gitignore`
- Create: `.claude-plugin/plugin.json`

- [ ] **Step 1: .gitignore 작성**

```gitignore
.grounded/
__pycache__/
*.pyc
.DS_Store
```

- [ ] **Step 2: 플러그인 매니페스트 작성**

```json
{
  "name": "grounded",
  "description": "Grounding enforcement guardrail — deterministically blocks ungrounded agent actions (editing files it never read). Not a security tool; a grounding tool.",
  "version": "0.1.0"
}
```

`.claude-plugin/plugin.json` 경로에 저장.

- [ ] **Step 3: 초기 커밋**

```bash
git add .gitignore .claude-plugin/plugin.json grounded-spec.html CLAUDE.md docs/
git commit -m "chore: scaffold grounded v0.1 — spec, plugin manifest, plan"
```

---

### Task 2: exit 2 self-test (명세 08-1 — 가장 먼저)

"내 환경에서 PreToolUse exit 2가 진짜 도구를 막는가"를 1줄 hook으로 검증. 실패하면 이후 작업 전면 중단하고 HTTP hook 방식 검토 (명세 리스크 표 1행).

**Files:**
- Create: 없음 (임시 디렉토리에서 검증 후 폐기)

- [ ] **Step 1: 임시 프로젝트에 차단 전용 1줄 hook 등록**

```bash
rm -rf /tmp/grounded-selftest && mkdir -p /tmp/grounded-selftest/.claude
echo "hello" > /tmp/grounded-selftest/target.txt
cat > /tmp/grounded-selftest/.claude/settings.json <<'EOF'
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "echo '[grounded selftest] PreToolUse exit 2 block' >&2; exit 2" }
        ]
      }
    ]
  }
}
EOF
```

- [ ] **Step 2: 헤드리스 Claude로 Edit 시도시켜 차단 확인**

```bash
cd /tmp/grounded-selftest && claude -p "Use the Edit tool to replace hello with goodbye in target.txt" --allowedTools "Read,Edit" 2>&1 | tail -5
cat /tmp/grounded-selftest/target.txt
```

Expected: `target.txt` 내용이 여전히 `hello` (변경 안 됨). 출력에 차단 흔적.
**FAIL이면(파일이 goodbye로 바뀜): 여기서 중단. 사용자에게 보고하고 HTTP hook 전환 논의.**

- [ ] **Step 3: 임시 디렉토리 정리**

```bash
rm -rf /tmp/grounded-selftest
```

---

### Task 3: ledger_io — 공유 ledger 모듈

**Files:**
- Create: `hooks/ledger_io.py`
- Test: `tests/hook_runner.py`, `tests/test_pre_gate.py` 등에서 간접 검증 (subprocess 경유라 ledger_io 단독 유닛테스트는 생략 — hook 테스트가 전부 ledger_io를 통과함)

- [ ] **Step 1: 테스트 헬퍼 작성** (`tests/hook_runner.py`)

```python
"""Run a hook script the way Claude Code does: subprocess + JSON on stdin."""
import json
import os
import subprocess
import sys

HOOKS_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "hooks")


def run_hook(script, payload):
    return subprocess.run(
        [sys.executable, os.path.join(HOOKS_DIR, script)],
        input=json.dumps(payload),
        capture_output=True,
        text=True,
    )
```

- [ ] **Step 2: ledger_io 구현** (`hooks/ledger_io.py`)

```python
"""Shared ledger I/O for grounded hooks.

Hooks are stateless one-shot processes; this file IS the session state.
Philosophy (spec §05): false positives are worse than misses — when the
ledger is unreadable, callers fail open.
"""
import json
import os
import tempfile

LEDGER_DIR = ".grounded"
LEDGER_FILE = "ledger.json"


def default_ledger():
    return {"read_files": {}, "verified_urls": {}, "known_pkgs": {}}


def ledger_path(cwd):
    return os.path.join(cwd, LEDGER_DIR, LEDGER_FILE)


def load_ledger(cwd):
    """Ledger dict; default if the file is absent; None if corrupt."""
    try:
        with open(ledger_path(cwd), encoding="utf-8") as f:
            data = json.load(f)
    except FileNotFoundError:
        return default_ledger()
    except (json.JSONDecodeError, OSError, UnicodeDecodeError):
        return None
    if not isinstance(data, dict):
        return None
    merged = default_ledger()
    for key in merged:
        if isinstance(data.get(key), dict):
            merged[key] = data[key]
    return merged


def save_ledger(cwd, ledger):
    """Atomic replace so parallel hook invocations never leave partial JSON."""
    path = ledger_path(cwd)
    os.makedirs(os.path.dirname(path), exist_ok=True)
    fd, tmp = tempfile.mkstemp(dir=os.path.dirname(path), suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            json.dump(ledger, f, ensure_ascii=False, indent=2)
        os.replace(tmp, path)
    except OSError:
        try:
            os.unlink(tmp)
        except OSError:
            pass


def normalize(path, cwd):
    """Absolute, symlink-resolved path so Read('./a.py') grounds Edit('/abs/a.py')."""
    if not os.path.isabs(path):
        path = os.path.join(cwd, path)
    return os.path.realpath(path)
```

- [ ] **Step 3: 커밋**

```bash
git add hooks/ledger_io.py tests/hook_runner.py
git commit -m "feat: ledger I/O — atomic shared session state for hooks"
```

---

### Task 4: session_start — ledger 초기화 hook

**Files:**
- Create: `hooks/session_start.py`
- Test: `tests/test_session_start.py`

- [ ] **Step 1: 실패하는 테스트 작성** (`tests/test_session_start.py`)

```python
import json
import os
import tempfile
import unittest

from hook_runner import run_hook


def payload(cwd, source):
    return {"hook_event_name": "SessionStart", "cwd": cwd, "source": source}


class SessionStartTest(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.cwd = os.path.realpath(self.tmp.name)
        self.ledger = os.path.join(self.cwd, ".grounded", "ledger.json")

    def tearDown(self):
        self.tmp.cleanup()

    def read_ledger(self):
        with open(self.ledger) as f:
            return json.load(f)

    def seed_ledger(self, read_files):
        os.makedirs(os.path.dirname(self.ledger), exist_ok=True)
        with open(self.ledger, "w") as f:
            json.dump({"read_files": read_files, "verified_urls": {}, "known_pkgs": {}}, f)

    def test_startup_creates_empty_ledger(self):
        r = run_hook("session_start.py", payload(self.cwd, "startup"))
        self.assertEqual(r.returncode, 0)
        self.assertEqual(self.read_ledger(), {"read_files": {}, "verified_urls": {}, "known_pkgs": {}})

    def test_startup_resets_existing_ledger(self):
        self.seed_ledger({"/old/file.py": 1})
        run_hook("session_start.py", payload(self.cwd, "startup"))
        self.assertEqual(self.read_ledger()["read_files"], {})

    def test_clear_resets_existing_ledger(self):
        self.seed_ledger({"/old/file.py": 1})
        run_hook("session_start.py", payload(self.cwd, "clear"))
        self.assertEqual(self.read_ledger()["read_files"], {})

    def test_resume_keeps_existing_ledger(self):
        self.seed_ledger({"/kept/file.py": 1})
        run_hook("session_start.py", payload(self.cwd, "resume"))
        self.assertEqual(self.read_ledger()["read_files"], {"/kept/file.py": 1})

    def test_resume_with_corrupt_ledger_heals_to_empty(self):
        os.makedirs(os.path.dirname(self.ledger), exist_ok=True)
        with open(self.ledger, "w") as f:
            f.write("{not json")
        r = run_hook("session_start.py", payload(self.cwd, "resume"))
        self.assertEqual(r.returncode, 0)
        self.assertEqual(self.read_ledger()["read_files"], {})

    def test_garbage_stdin_exits_zero(self):
        import subprocess, sys
        from hook_runner import HOOKS_DIR
        r = subprocess.run([sys.executable, os.path.join(HOOKS_DIR, "session_start.py")],
                           input="not json", capture_output=True, text=True)
        self.assertEqual(r.returncode, 0)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest discover -s tests -p "test_session_start.py" -v`
Expected: FAIL/ERROR (session_start.py 없음)

- [ ] **Step 3: 구현** (`hooks/session_start.py`)

```python
"""SessionStart hook: initialize .grounded/ledger.json.

Reset on startup/clear. Keep on resume/compact — the conversation still
remembers those reads; wiping them would cause false blocks (spec §05).
Never blocks: always exit 0.
"""
import json
import sys

import ledger_io


def main():
    try:
        payload = json.load(sys.stdin)
    except (json.JSONDecodeError, UnicodeDecodeError):
        return 0
    cwd = payload.get("cwd") or "."
    source = payload.get("source") or "startup"
    ledger = None
    if source in ("resume", "compact"):
        ledger = ledger_io.load_ledger(cwd)  # None if corrupt
    if ledger is None:
        ledger = ledger_io.default_ledger()
    ledger_io.save_ledger(cwd, ledger)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python3 -m unittest discover -s tests -p "test_session_start.py" -v`
Expected: PASS (6 tests OK)

- [ ] **Step 5: 커밋**

```bash
git add hooks/session_start.py tests/test_session_start.py
git commit -m "feat: SessionStart hook — init ledger, preserve on resume"
```

---

### Task 5: post_record — 근거 적립 hook

**Files:**
- Create: `hooks/post_record.py`
- Test: `tests/test_post_record.py`

- [ ] **Step 1: 실패하는 테스트 작성** (`tests/test_post_record.py`)

```python
import json
import os
import tempfile
import unittest

from hook_runner import run_hook


def payload(tool_name, tool_input, cwd):
    return {"hook_event_name": "PostToolUse", "tool_name": tool_name,
            "tool_input": tool_input, "tool_response": {}, "cwd": cwd}


class PostRecordTest(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.cwd = os.path.realpath(self.tmp.name)
        self.ledger = os.path.join(self.cwd, ".grounded", "ledger.json")

    def tearDown(self):
        self.tmp.cleanup()

    def touch(self, name):
        p = os.path.join(self.cwd, name)
        with open(p, "w") as f:
            f.write("x")
        return p

    def read_files(self):
        with open(self.ledger) as f:
            return json.load(f)["read_files"]

    def test_read_records_file_with_timestamp(self):
        p = self.touch("a.py")
        r = run_hook("post_record.py", payload("Read", {"file_path": p}, self.cwd))
        self.assertEqual(r.returncode, 0)
        self.assertIn(p, self.read_files())
        self.assertIsInstance(self.read_files()[p], int)

    def test_creates_ledger_dir_if_missing(self):
        p = self.touch("a.py")
        run_hook("post_record.py", payload("Read", {"file_path": p}, self.cwd))
        self.assertTrue(os.path.exists(self.ledger))

    def test_write_grounds_the_written_file(self):
        p = self.touch("made.py")
        run_hook("post_record.py", payload("Write", {"file_path": p, "content": "x"}, self.cwd))
        self.assertIn(p, self.read_files())

    def test_relative_path_is_normalized_to_absolute(self):
        p = self.touch("a.py")
        run_hook("post_record.py", payload("Read", {"file_path": "a.py"}, self.cwd))
        self.assertIn(p, self.read_files())

    def test_grep_on_single_file_records_it(self):
        p = self.touch("a.py")
        run_hook("post_record.py", payload("Grep", {"pattern": "x", "path": p}, self.cwd))
        self.assertIn(p, self.read_files())

    def test_grep_on_directory_records_nothing(self):
        run_hook("post_record.py", payload("Grep", {"pattern": "x", "path": self.cwd}, self.cwd))
        self.assertFalse(os.path.exists(self.ledger))

    def test_bash_cat_records_existing_file(self):
        p = self.touch("a.py")
        run_hook("post_record.py", payload("Bash", {"command": f"cat {p}"}, self.cwd))
        self.assertIn(p, self.read_files())

    def test_bash_cat_ignores_missing_file(self):
        run_hook("post_record.py", payload("Bash", {"command": "cat /no/such/file.py"}, self.cwd))
        self.assertFalse(os.path.exists(self.ledger))

    def test_unrelated_bash_records_nothing(self):
        run_hook("post_record.py", payload("Bash", {"command": "ls -la"}, self.cwd))
        self.assertFalse(os.path.exists(self.ledger))

    def test_corrupt_ledger_is_healed_not_crashed(self):
        os.makedirs(os.path.dirname(self.ledger), exist_ok=True)
        with open(self.ledger, "w") as f:
            f.write("{not json")
        p = self.touch("a.py")
        r = run_hook("post_record.py", payload("Read", {"file_path": p}, self.cwd))
        self.assertEqual(r.returncode, 0)
        self.assertIn(p, self.read_files())
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest discover -s tests -p "test_post_record.py" -v`
Expected: FAIL/ERROR (post_record.py 없음)

- [ ] **Step 3: 구현** (`hooks/post_record.py`)

```python
"""PostToolUse hook: record evidence into the ledger.

Observe-only — never blocks, always exit 0 (spec §03: post-record 적립,
pre-gate 검문). Evidence sources for G-1: Read, Grep(single file),
Edit/Write (you know content you just wrote), Bash `cat`.
"""
import json
import os
import re
import shlex
import sys
import time

import ledger_io

RECORDING_TOOLS = {"Read", "Edit", "Write", "MultiEdit", "NotebookEdit"}
CAT_SEGMENT = re.compile(r"(?:^|[|;&]\s*)cat\b([^|;&><]*)")


def cat_targets(command, cwd):
    """File args of `cat` invocations that actually exist on disk."""
    found = []
    for match in CAT_SEGMENT.finditer(command):
        try:
            tokens = shlex.split(match.group(1))
        except ValueError:
            continue
        for tok in tokens:
            if tok.startswith("-"):
                continue
            if os.path.isfile(ledger_io.normalize(tok, cwd)):
                found.append(tok)
    return found


def extract_paths(tool_name, tool_input, cwd):
    raw = []
    if tool_name in RECORDING_TOOLS:
        p = tool_input.get("file_path") or tool_input.get("notebook_path")
        if p:
            raw.append(p)
    elif tool_name == "Grep":
        p = tool_input.get("path")
        if p and os.path.isfile(ledger_io.normalize(p, cwd)):
            raw.append(p)
    elif tool_name == "Bash":
        raw.extend(cat_targets(tool_input.get("command") or "", cwd))
    return [ledger_io.normalize(p, cwd) for p in raw]


def main():
    try:
        payload = json.load(sys.stdin)
    except (json.JSONDecodeError, UnicodeDecodeError):
        return 0
    cwd = payload.get("cwd") or "."
    tool_input = payload.get("tool_input") or {}
    paths = extract_paths(payload.get("tool_name") or "", tool_input, cwd)
    if not paths:
        return 0
    ledger = ledger_io.load_ledger(cwd)
    if ledger is None:  # corrupt: heal with a fresh ledger
        ledger = ledger_io.default_ledger()
    now = int(time.time())
    for p in paths:
        ledger["read_files"][p] = now
    ledger_io.save_ledger(cwd, ledger)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python3 -m unittest discover -s tests -p "test_post_record.py" -v`
Expected: PASS (10 tests OK)

- [ ] **Step 5: 커밋**

```bash
git add hooks/post_record.py tests/test_post_record.py
git commit -m "feat: PostToolUse hook — accrue read evidence into ledger"
```

---

### Task 6: pre_gate — G-1 검문 hook

**Files:**
- Create: `hooks/pre_gate.py`
- Test: `tests/test_pre_gate.py`

- [ ] **Step 1: 실패하는 테스트 작성** (`tests/test_pre_gate.py`)

```python
import json
import os
import tempfile
import unittest

from hook_runner import run_hook


def payload(tool_name, file_path, cwd):
    return {"hook_event_name": "PreToolUse", "tool_name": tool_name,
            "tool_input": {"file_path": file_path}, "cwd": cwd}


class PreGateTest(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.cwd = os.path.realpath(self.tmp.name)

    def tearDown(self):
        self.tmp.cleanup()

    def touch(self, name):
        p = os.path.join(self.cwd, name)
        with open(p, "w") as f:
            f.write("x")
        return p

    def write_ledger(self, read_files):
        d = os.path.join(self.cwd, ".grounded")
        os.makedirs(d, exist_ok=True)
        with open(os.path.join(d, "ledger.json"), "w") as f:
            json.dump({"read_files": read_files, "verified_urls": {}, "known_pkgs": {}}, f)

    def test_edit_unread_existing_file_blocked_exit2(self):
        p = self.touch("a.py")
        self.write_ledger({})
        r = run_hook("pre_gate.py", payload("Edit", p, self.cwd))
        self.assertEqual(r.returncode, 2)
        self.assertIn("a.py", r.stderr)
        self.assertIn("grounded", r.stderr)

    def test_edit_read_file_passes_silently(self):
        p = self.touch("a.py")
        self.write_ledger({p: 1718000000})
        r = run_hook("pre_gate.py", payload("Edit", p, self.cwd))
        self.assertEqual(r.returncode, 0)
        self.assertEqual(r.stderr, "")

    def test_write_new_file_passes(self):
        self.write_ledger({})
        r = run_hook("pre_gate.py", payload("Write", os.path.join(self.cwd, "new.py"), self.cwd))
        self.assertEqual(r.returncode, 0)

    def test_write_existing_unread_file_blocked(self):
        p = self.touch("a.py")
        self.write_ledger({})
        r = run_hook("pre_gate.py", payload("Write", p, self.cwd))
        self.assertEqual(r.returncode, 2)

    def test_relative_path_matches_absolute_ledger_entry(self):
        p = self.touch("a.py")
        self.write_ledger({p: 1718000000})
        r = run_hook("pre_gate.py", payload("Edit", "a.py", self.cwd))
        self.assertEqual(r.returncode, 0)

    def test_missing_ledger_blocks_edit(self):
        p = self.touch("a.py")
        r = run_hook("pre_gate.py", payload("Edit", p, self.cwd))
        self.assertEqual(r.returncode, 2)

    def test_corrupt_ledger_fails_open(self):
        p = self.touch("a.py")
        d = os.path.join(self.cwd, ".grounded")
        os.makedirs(d, exist_ok=True)
        with open(os.path.join(d, "ledger.json"), "w") as f:
            f.write("{not json")
        r = run_hook("pre_gate.py", payload("Edit", p, self.cwd))
        self.assertEqual(r.returncode, 0)

    def test_ungated_tool_passes(self):
        r = run_hook("pre_gate.py", {"hook_event_name": "PreToolUse", "tool_name": "Bash",
                                     "tool_input": {"command": "ls"}, "cwd": self.cwd})
        self.assertEqual(r.returncode, 0)

    def test_garbage_stdin_fails_open(self):
        import subprocess, sys
        from hook_runner import HOOKS_DIR
        r = subprocess.run([sys.executable, os.path.join(HOOKS_DIR, "pre_gate.py")],
                           input="not json", capture_output=True, text=True)
        self.assertEqual(r.returncode, 0)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest discover -s tests -p "test_pre_gate.py" -v`
Expected: FAIL/ERROR (pre_gate.py 없음)

- [ ] **Step 3: 구현** (`hooks/pre_gate.py`)

```python
"""PreToolUse hook: G-1 read-before-edit gate.

exit 0 = pass, exit 2 = block (stderr is fed back to the model).
Spec §05 — 오탐 < 누락: when state is unreadable, fail open; block only
when the absence of evidence is unambiguous.
"""
import json
import os
import sys

import ledger_io

GATED_TOOLS = {"Edit", "Write", "MultiEdit", "NotebookEdit"}


def main():
    try:
        payload = json.load(sys.stdin)
    except (json.JSONDecodeError, UnicodeDecodeError):
        return 0
    if (payload.get("tool_name") or "") not in GATED_TOOLS:
        return 0
    tool_input = payload.get("tool_input") or {}
    raw = tool_input.get("file_path") or tool_input.get("notebook_path")
    if not raw:
        return 0
    cwd = payload.get("cwd") or "."
    path = ledger_io.normalize(raw, cwd)
    if payload.get("tool_name") == "Write" and not os.path.exists(path):
        return 0  # creating a brand-new file needs no prior read
    ledger = ledger_io.load_ledger(cwd)
    if ledger is None:
        return 0  # corrupt ledger: fail open rather than false-block
    if path in ledger["read_files"]:
        return 0
    sys.stderr.write(
        f"[grounded G-1] No record of reading {raw} in this session. "
        "Do not edit from guesswork — read the file with the Read tool first, "
        "then retry.\n"
    )
    return 2


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 전체 테스트 통과 확인**

Run: `python3 -m unittest discover -s tests -v`
Expected: PASS (25 tests OK)

- [ ] **Step 5: 커밋**

```bash
git add hooks/pre_gate.py tests/test_pre_gate.py
git commit -m "feat: PreToolUse hook — G-1 gate, exit 2 on unread edit"
```

---

### Task 7: hook 등록 — 개발용 settings.json + 플러그인용 hooks.json

**Files:**
- Create: `.claude/settings.json` (개발/도그푸딩)
- Create: `hooks/hooks.json` (플러그인 패키징)

- [ ] **Step 1: 개발용 등록** (`.claude/settings.json`)

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "python3 \"$CLAUDE_PROJECT_DIR/hooks/session_start.py\"" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Read|Grep|Edit|Write|MultiEdit|NotebookEdit|Bash",
        "hooks": [
          { "type": "command", "command": "python3 \"$CLAUDE_PROJECT_DIR/hooks/post_record.py\"" }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          { "type": "command", "command": "python3 \"$CLAUDE_PROJECT_DIR/hooks/pre_gate.py\"" }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 플러그인용 등록** (`hooks/hooks.json`) — 동일 구조, 경로 변수만 `${CLAUDE_PLUGIN_ROOT}`

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.py\"" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Read|Grep|Edit|Write|MultiEdit|NotebookEdit|Bash",
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/post_record.py\"" }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/pre_gate.py\"" }
        ]
      }
    ]
  }
}
```

- [ ] **Step 3: 커밋**

```bash
git add .claude/settings.json hooks/hooks.json
git commit -m "feat: register hooks — settings.json (dev) + hooks.json (plugin)"
```

---

### Task 8: E2E 라이브 검증 — 실제 에이전트가 막히고, 읽으면 통과하는가

**Files:** 임시 파일만 (커밋 안 함)

- [ ] **Step 1: 안 읽고 수정 → 차단 확인**

```bash
cd /Users/hyde/Documents/grounded
echo "alpha" > e2e_target.txt
claude -p "Use the Edit tool to change alpha to beta in e2e_target.txt. Do NOT read the file first, edit it directly." --allowedTools "Edit" 2>&1 | tail -5
cat e2e_target.txt
```

Expected: `e2e_target.txt`가 여전히 `alpha`. (Read가 금지돼 있어 모델이 우회 불가)

- [ ] **Step 2: 읽은 뒤 수정 → 통과 확인**

```bash
claude -p "Read e2e_target.txt, then use the Edit tool to change alpha to beta." --allowedTools "Read,Edit" 2>&1 | tail -5
cat e2e_target.txt
cat .grounded/ledger.json
```

Expected: `e2e_target.txt`가 `beta`. ledger의 `read_files`에 e2e_target.txt 절대경로 존재.

- [ ] **Step 3: 정리 + 최종 커밋**

```bash
rm -f e2e_target.txt
git status --short   # .grounded/ 는 gitignore로 제외 확인
git add -A && git commit -m "docs: v0.1 G-1 e2e verified" --allow-empty
```

---

## Self-Review 결과

- **명세 08(MVP) 커버리지:** ① exit 2 확인 → Task 2 / ② ledger 초기화 → Task 4 / ③ 적립 → Task 5 / ④ 검문 → Task 6 / ⑤ settings.json 등록 → Task 7. 전부 매핑됨. 플러그인 출시 결정(사용자 추가 요구) → Task 1·7의 plugin.json + hooks.json.
- **placeholder 스캔:** 없음 — 전 코드/명령 완전 기재.
- **타입 일관성:** `ledger_io.load_ledger`(absent→default, corrupt→None) 의미를 session_start/post_record/pre_gate가 동일하게 사용. `normalize(path, cwd)` 시그니처 전 태스크 일치.
